import palavras from './palavras';


export default function onPlay(letraSelecionada) {
    if (palavras[0].contains(letraSelecionada)) {
        // mostrar o caractere no underline

        // desativar botão

    }
}